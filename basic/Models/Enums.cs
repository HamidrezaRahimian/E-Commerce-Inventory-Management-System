namespace ECommerceInventorySystem.Models
{
    public enum ProductCategory { Electronics, Clothing, Food, HomeGarden, Sports, Books }
    public enum StockStatus { InStock, LowStock, OutOfStock, Discontinued, Backordered }
    public enum WarehouseLocation { MainWarehouse, RegionalCenter, ReturnCenter }
    public enum ProductCondition { New, Refurbished, OpenBox, Damaged }
}
